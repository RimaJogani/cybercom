var firstName='Rima';
var age=20;

if(age < 13)
{
	console.log(firstName + ' is Girl');
}
else if(age >= 13 && age < 20)
{
	console.log(firstName + ' is teenager');
}
else if(age <= 20 && age < 30)
{
	console.log(firstName + ' is young women');
}
else
{
	console.log(firstName + ' is women');
}